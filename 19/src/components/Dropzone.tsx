import React, { useCallback, useState } from 'react';
import { useDropzone } from 'react-dropzone';

interface FileObject {
  name: string;
  size: number;
  type: string;
  lastModified: number;
}

const DropzoneComponent: React.FC = () => {
 
 

  return (
    <div>
      not working...i can try later.
    </div>
  );
};

export default DropzoneComponent;
